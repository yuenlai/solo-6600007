# solo-6600007 - Audio Fingerprint Music Recognizer

## Tech
- **Frontend**: React + TypeScript + Zustand
- **Backend**: Rust + Actix-web + FFT (rustfft)
- **Database**: SQLite

## Start
```bash
cd backend && cargo run
cd frontend && npm install && npm run dev
```
